name: CI Checks

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  issues: write         # Necessary to write a comment

jobs:
  call_reusable_ci:
    name: Standardized CI Checks
    uses: marcusburghardt/github-standards/.github/workflows/reusable_ci.yml@main
